Part 1 – Backend Implementation (60 Minutes)
Build a simple backend service with one API endpoint.
Scenario
You are building a simplified transaction risk assessment service.
Requirements
Create a REST API with the following endpoint:
POST /assess-risk
Request Body
Functional Requirements
Validate all input fields properly.
Store the request in a data base (SQLite or PostgreSQL).
Implement basic risk logic:
If transaction_amount > 10,000 → HIGH risk
If country is not in an allowed list (e.g., CA, US, UK) → MEDIUM risk
Otherwise → LOW risk
Return a structured JSON response:
Expectations
Clean project structure
Proper error handling
Meaningful HTTP status codes
Clear separation of concerns
Simple README with setup/run instructions
You may only use Django or FastAPI backend framework

Part 2 – Architecture & System Design (30 Minutes)
Provide written responses (concise but structured) to the following:
How would you scale this service to handle 10,000+ requests per second?
If this service integrates with an external ML model, how would you:
Control latency?
Prevent cascading failures?
Implement fallback logic?
How would you deploy this service in AWS or Azure for production reliability?
Be specific in your technical reasoning.
Submission Requirements
Git repository link (public or temporary access)
README with instructions
